{
  "name": "sticky-notes",
  "version": "0.3.0",
  "private": true,
  "description": "A lightweight desktop sticky notes app built with Tauri.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml -p note-store"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
